interface:
  display_name: "RaidersRouter"
  short_description: "Repo-specific patterns and workflows for RaidersRouter"
  default_prompt: "Use the RaidersRouter repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true