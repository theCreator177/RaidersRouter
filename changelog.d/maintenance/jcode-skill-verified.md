- **chore(skills):** rewrite the `jcode` skill against a verified v0.61.1 install instead of README claims — documents the non-interactive `jcode run --json/--ndjson` delegation path (the original wrongly implied jcode was interactive-only), the ~45-provider routing including `openai-compatible` for pointing jcode at a local OmniRoute gateway, telemetry and auto-update both defaulting ON with their opt-outs, installer behaviour (no sudo, SHA-256 verified, `~/.local/bin`, ~138 MB), and measured peak RSS of 23.4 MB versus the README's ~27.8 MB claim.
