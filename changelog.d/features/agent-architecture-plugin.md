- **feat(skills):** add the `agent-architecture` plugin — two skills for building and debugging AI agents as three separate systems rather than one blob: `loop-graph-harness` (the LOOP controls repetition, the GRAPH controls topology, the HARNESS controls reality; design outside-in) and `agent-failure-triage` (route a symptom to the layer that owns it — never stops → loop, cannot see why → graph, can touch anything → harness). Bundles a tooling reference covering Temporal, LangGraph, NetworkX, E2B, OpenAI Evals and OpenTelemetry with adoption guidance. Transcribed from a public post by [@elune0x](https://x.com/elune0x/status/2082133200386555918).
