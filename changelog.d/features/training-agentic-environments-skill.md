- **feat(skills):** add `training-agentic-environments` to the `agent-architecture` plugin — how to design RL environments and rewards that actually produce autonomous agents rather than metric-gamers: grade the final environment state instead of the agent's self-reported completion, isolate agents from verifiers and pair public diagnostic verifiers with hidden held-out ones, randomize the agent harness across tasks so the policy does not overfit one tool schema, make budget overrun cost reward (`-1`) rather than merely truncate, and run untrusted agent code in something that survives hostile behavior. Sourced from the [Kimi K3 technical report](https://github.com/MoonshotAI/Kimi-K3/blob/main/k3_tech_report.pdf) §4.1–4.2 and §5.3.
