- **feat(skills):** add the `graph-loop` skill to `raiders-toolkit` — a 12-step method for turning a flat pile of notes/docs into a graph the model walks in milliseconds: a `<500`-token router (`CLAUDE.md`), a one-line-per-node index, one-idea nodes, sharp edges, and retrieval implemented as deterministic code so the model fires once at the end. Transcribed from a public infographic by [@kingwilliam_](https://x.com/kingwilliam_/status/2080359562679349316).
